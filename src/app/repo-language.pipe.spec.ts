import { RepoLanguagePipe } from './repo-language.pipe';

describe('RepoLanguagePipe', () => {
  it('create an instance', () => {
    const pipe = new RepoLanguagePipe();
    expect(pipe).toBeTruthy();
  });
});
